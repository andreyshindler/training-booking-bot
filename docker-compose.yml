services:
  bot:
    build: .
    container_name: training-booking-bot
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/bookings.db
    volumes:
      - bot-data:/data

volumes:
  bot-data:
